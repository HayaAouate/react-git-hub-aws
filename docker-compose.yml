version: '3'
services:
  web:
    build: .
    ports:
      - "3000:3000"  # Lier le port 3000 du conteneur au port 3000 de la machine hôte
    restart: always
    volumes:
      - .:/app
    environment:
      - NODE_ENV=development
